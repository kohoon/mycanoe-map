# 2026-09-10

## 한 일

- 관리자 지형지물 유형에 `🍽️ 식당/카페`를 추가했다.
- 식당/카페는 이름을 필수로 입력하고, 카카오맵에서 공유한 장소 URL을 선택적으로 저장할 수 있게 했다.
- 식당/카페 마커는 일반 사용자도 상세 팝업을 열 수 있다. 팝업에는 이름·메모와 `카카오맵 장소 상세` 링크가 표시된다.
- 카카오맵 URL을 입력하지 않은 경우에도 등록 이름과 좌표를 이용한 카카오맵 위치 링크를 자동 생성한다.
- 외부 링크 오용을 막기 위해 HTTPS의 `map.kakao.com`, `place.map.kakao.com`, `kko.to`만 클라이언트와 Worker에서 허용한다.
- 카카오 API가 반환하는 HTTP 장소 URL이나 HTTP 공유 링크는 허용 도메인을 확인한 뒤 HTTPS로 정규화한다.
- 식당/카페 URL을 비워 저장하면 Worker가 이름과 등록 좌표로 카카오 로컬 키워드 검색을 실행하고, 식당·카페
  결과 중 이름 유사도와 거리가 기준을 통과한 장소의 상세 URL을 자동 저장하도록 확장했다.
- 자동 매칭 결과에는 카카오 장소 ID·실제 장소명·거리·매칭시각을 함께 남기며, 팝업에도 매칭된 이름과 거리를 표시한다.
- 정확히 같은 이름은 3km, 부분·유사 이름은 1.5km 이내로 제한하고, 일치하지 않으면 종전 좌표 링크를 사용한다.
- 자동 연결된 항목을 수정할 때는 기존 자동 URL을 직접입력란에 고정하지 않고, 바뀐 이름·좌표로 다시 검색한다.
  관리자가 직접 입력한 URL은 그대로 유지한다.
- 행정안전부 생활안전지도 물놀이 관리지역 `IF_0044`를 전수 재수집했다. 공개 최신본은 여전히 2025년 수집·
  2025-07-25 수정본 1,336건이며, 2026-09-10 API 응답도 1,336건으로 기존 원본과 같았다.
- 1,336건 모두를 주소 검색점, 현재 좌표의 행정구역, 거리, 같은 좌표 반복 여부로 검사하고 이상 후보 207건은
  장소명 검색을 추가 대조했다.
- 장소명·법정리·교량/계곡 또는 하천 중심선이 함께 확인된 32건을 새로 보정했다. 기존 북벽앞·파서탕을 포함한
  좌표 보정은 총 34건이다. 특히 충주에 있던 배알도공원을 광양으로 옮기고, 한 점에 겹친 양평·철원·인제·단양
  관리지역들을 실제 장소별로 분리했다.
- 좌표 전수 점검 도구와 좌표/근거 회귀 테스트를 추가하고, 데이터 메타에 원천 수정일·최신 확인일·보정 건수를
  기록했다. 상세 보고서는 `docs/waterplay-coordinate-audit-2026-09-10.md`에 남겼다.
- 영산강 승촌보 수상레저 금지구역의 오배치를 보정했다. 기존 기준점을 실제 승촌보 지명
  중심으로 옮기고 영산강 물길 상·하류 각 1km를 다시 생성했다.
- 해경 공식 현황표에 승촌보의 금지대상 기구가 적혀 있지 않은데도 `모든 수상레저기구`로 표시하던
  추정을 제거했다. 대상 미기재 구역은 카누 가능으로 오판하지 않고 `카누 적용 여부 확인 필요`로 보여준다.

## 결정·근거

- 기존 보·여울 등 안전 지형지물은 일반 사용자에게 말풍선을 열지 않는 정책을 유지한다. 장소 정보가 필요한 식당/카페만 공개 상세 팝업을 사용한다.
- 지형지물 저장 형식에 선택 필드 `kakaoUrl`을 추가해 기존 데이터와 호환되게 했다.
- 카카오 장소 ID는 매칭 이력을 추적하기 위한 값일 뿐 현재 영업 상태로 판정하지 않는다. 사용자는 상세 링크에서
  카카오맵의 현재 정보를 확인한다.
- 자동 보 시드용 `weir:` 예외는 유형이 `보`일 때만 허용해, 식당/카페 등록과 카카오 검색은 반드시 관리자 인증 뒤 실행되게 했다.
- 물놀이 주소가 읍·면까지만 제공되는 행은 주소 검색점과 수 km 차이가 나도 오류로 단정하지 않았다. 정확한
  장소명이나 법정리, 실제 교량·계곡·하천 중심선이 함께 맞는 고신뢰 사례만 이동하고 나머지는 원본점을 유지했다.
- 같은 좌표의 여러 ID는 과거/현재 행 또는 상·하류 복수 관리구간일 수 있어 자동 병합하지 않았다. 식별자 존재를
  현재 동일 상태로 해석하지 않고 원본 ID와 관리 속성을 보존했다.
- 승촌보 구역은 해경이 현재 게시한 2024년 기준 내수면 현황표의 `승촌보에서 상·하류
  1km`, `영구`를 정본으로 삼았다. 금지대상 빈칸을 다른 행의 값으로 보완하지 않았다.

## 검증

- `python3 tools/build_map.py`, `python3 tools/build_map.py test`, 생성 HTML 4개의 인라인 JavaScript `node --check`,
  Worker `node --check`와 Wrangler dry-run 번들을 통과했다.
- Playwright 모바일 검증에서 지형지물 유형 9개와 3열 선택 UI, 식당/카페 선택 시 이름·카카오 URL 입력란 표시,
  이름 누락·허용되지 않은 URL 오류 안내, 저장 요청의 `type/name/kakaoUrl`을 확인했다.
- 일반 사용자 상태에서 식당/카페만 상세 팝업을 유지하고 여울은 팝업이 없는 기존 정책, 입력 URL 및 좌표 폴백
  카카오맵 링크, 외부 유사 도메인 거부와 JavaScript 오류 0건을 확인했다.
- Wrangler 로컬 Worker에서 식당/카페 저장·조회는 200, 허용되지 않은 URL과 이름 누락은 각각 400 응답임을 확인했다.
- 프로젝트의 카카오 REST 키로 공식 로컬 키워드 검색 API가 200 응답과 `place_url`·장소 ID·거리 정보를 반환하는지 확인했다.
- Wrangler 로컬 Worker 실API 검증에서 `스타벅스 코엑스몰점`은 157m 거리의 카카오 장소 ID `23707172`와
  HTTPS 상세 URL로 자동 연결됐다. 존재하지 않는 이름은 URL 없이 저장돼 좌표 폴백이 유지됐고, 직접 지정 URL은 자동 검색 없이 보존됐다.
- 카카오맵 HTTP 직접 지정 URL이 같은 장소의 HTTPS URL로 저장되는 것도 로컬 Worker에서 확인했다.
- `weir:` ID를 흉내 낸 무권한 식당/카페 등록은 403, 정상 보 시드는 200인지 음성·양성 회귀 검증했다.
- Playwright 모바일 검증에서 URL 입력을 비워 보낸 저장 요청, Worker 자동 매칭 응답의 마커 반영,
  팝업의 매칭 이름·거리·상세 URL, `자동 연결 완료` 안내와 JavaScript 오류 0건을 확인했다.
- `python3 tools/build_waterplay.py` 재수집 후 `tests/test_waterplay_coordinates.py`와
  `tests/test_waterplay_staff.py`를 통과했다. 1,336개 ID 유일성·대한민국 좌표 범위·34개 보정 좌표 및 대표
  중복점 분리·배알도공원 위도 오기 회귀를 확인했다.
- 보정 후 전수 감사는 API 오류·역지오코딩 실패·시도 불일치·20km 이상 주소 이격이 모두 0건이었다.
  남은 시군 불일치 1건은 원본 주소의 `정성군` 오탈자이며 실제 점은 정선군 신월리다.
- `python3 tools/build_map.py`와 `python3 tools/build_map.py test`, 생성 HTML 4개의 인라인 JavaScript
  `node --check`를 통과했다.
- Playwright 모바일 크기에서 공개·호환·투어·테스트 HTML의 JavaScript 오류 0건, 물놀이 레이어 1,336개
  로드, 메타의 보정 34건, 배알도공원 좌표가 광양 위도 34.9598024로 표시되는 것을 확인했다.
- `tests/test_wlz_inland.py`에서 승촌보 피처 1개, 실제 보 중심점의 면 내부 포함, 상·하류 약 1km 범위,
  금지대상 미기재 속성을 회귀 검증했다. 다른 내수면 41개 피처는 기존 데이터와 동일하게 유지했다.
- 빌드 결과 수상레저 금지구역은 해수면 204개와 내수면 42개, 총 246개이며 생성 HTML 4개의 인라인
  JavaScript `node --check`를 통과했다.
- Playwright 모바일 크기에서 공개·호환·투어·테스트 HTML의 JavaScript 오류가 모두 0건이었다. 승촌보
  팝업·황갈 점선 스타일과 `미기재/모든 기구/동력만` 3상태 판정도 확인했다.
