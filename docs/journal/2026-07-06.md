# 2026-07-06

## 한 일
- **수위카드 1년 게이지 기준 정정** — 기존에는 `최근 1년 범위` 막대 위치가 최저~최고 선형 위치였고, 텍스트의 `1년 중 하위 %`는 일별 관측값 분포 백분위라 서로 다르게 보였다. 막대 위치도 백분위 기준으로 맞춰 텍스트와 그래프가 같은 기준을 나타내도록 수정.
- **광하교 수위 아이콘 표시 좌표 보정** — HRFCO 공식 관측소 좌표(`37.369722,128.618889`)는 유지하되, 지도 마커 표시 좌표를 VWorld 광하교 POI/다리 중심(`37.368204,128.619352`)으로 보정. `tools/build_hrfco.py`의 `COORD_OVERRIDES`에 남겨 재수집 시 원복되지 않게 처리.
- **누락 수위관측소 반영** — 현재 장소·코스 반경 12km 기준으로 `tools/build_hrfco.py`를 재실행해 기존 436곳에서 464곳으로 갱신. 양평/합천/금산·대전/하동·광양권 등 새로 선별된 28개 관측소를 포함.
- **남전교 수위 1시간 fallback** — `1010642 남전교`는 HRFCO 최신 `10M/<관측소>.json` 응답이 빈 배열이지만 `1H`/`1D` 자료는 존재. 수위 팝업 조회 시 10분 최신값이 없으면 최근 48시간 `1H` 최신값으로 보완하고 `YYYYMMDDHH` 시간도 표시하도록 수정.
- **수위 관련 추가 데이터 소스 리서치** — 현재 HRFCO/표준수문DB가 전국 하천 수위의 주 소스로 가장 적합함을 확인. 추가 구현 후보는 K-water 댐·보 수문 운영 정보(총방류량/유입량/저수율, 1순위), K-water 우량수위 관측소 운영 정보(HRFCO 보조/fallback, 2순위), 농어촌공사 저수지 수위정보(호수·저수지 장소용, 3순위), 국립해양조사원 조위관측소 최신 관측데이터(해안·하구 확장 시)로 정리해 `기획문서.md` 4-11b에 반영.

## 검증
- `python3 tools/build_map.py`
- `python3 tools/build_map.py test`
- `PYTHONPYCACHEPREFIX=.pycache_check python3 -m py_compile tools/*.py`
- `node --check /tmp/mycanoe_map_script.js`
- GitHub Pages 배포 `28768503567`: 최초 Pages 배포 단계 일시 실패(`Deployment failed, try again later`) 후 failed job 재실행으로 성공.
