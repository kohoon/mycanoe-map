# 2026-06-30

## 한 일
- **검색 미리보기 닫힘 수정** — 결과 선택 시 리스트 자동 닫힘 `[8e2beb6]`.
- **전기차 충전소 기능 1단계 구현 → 전면 롤백** — 한국환경공단 API 수집(`build_ev.py`)·3타입 토글·세부 모달까지 만들었으나 최종적으로 전부 제거 `[8e2beb6·bebcff2·728972d → a0df578]`.

## 결정 / 근거
- **EV 전면 제거 이유**:
  1. **테슬라/NACS 미포함** — 환경공단 전국 100,736개소 전부 확인했으나 테슬라 운영 충전소 0건(테슬라는 독자 운영·미보고). NACS 커넥터 코드(08~11)도 공식 코드표를 못 구해 확정 불가.
  2. **전국 임베드 부담** — "화면 내 모두 표시"하려면 bbox 조회 없는 API 특성상 전국(raw 44MB/gzip 5.2MB) 임베드 필요.
  3. **실시간 상태 불가(간편)** — 상태 API는 델타피드(최근 변동분만)라 특정 충전소 조회 불가 → Cron 폴러 필요.
- **EV API 노하우(제거했지만 기록)**: `B552584/EvCharger/getChargerInfo`, 전국 ~52만 충전기. **동시호출 차단(순차만)**, `numOfRows=9999`가 최적(52p ~15분, 페이지당 ~11~40s 변동). 상태는 `getChargerStatus`(델타).

## 미해결 / 다음 시작점
- EV는 종결(제거). 신규 기능 방향은 2026-07-01 저널 참조.

## 메모
- EV 삽질로 시간 많이 소요 — 데이터 출처의 한계(bbox 없음·델타피드·테슬라 미포함)를 초기에 검증했어야. 새 외부 데이터는 **도입 전 커버리지·조회방식부터 확인**.
