# 2026-06-26

## 한 일
- **날씨 고도화 분석** — data.go.kr 15084084(기상청 단기예보) 도입 검토.
- **강수형태 한글 표기 추가** — WMO 코드→한글 상태 매핑 `_wxText`(맑음/구름조금/흐림/안개/이슬비/비/눈/소나기/소나기눈/뇌우), 현재 날씨 칩에 표기(`☀️ 19° 맑음 …`). 기획문서 4-11c 갱신.
- **조안면 오배치 박스 제거** — 북한강 조안면 weir_zone 박스가 OSM 스냅으로 ~4km 북쪽 오배치. KCG 020501에 두물머리와 함께 그려져 `DUMUL_020501`이 이미 포함 → 박스 제거, '두물머리·조안면 구역'으로 통합 `[6148d23]`.
- **VWorld 도로명·지번 주소 검색** — Nominatim은 동·리까지만 → VWorld 검색 API(JSONP/VKEY: 도로명·지번·장소) 우선, 무결과·키없음 시 Nominatim 폴백 `[546be22]`.
- **관리자 KV 등록 지명 검색 포함** — 내 지도 검색에 지형지물 여울/유명지 이름("된꼬까리")·등록장소·KV코스 추가 `[74d5ee3]`.

## 결정 / 근거
- **기상청 단기예보(15084084) 직접 도입 보류** — 키 필수 + CORS 미보장이라 Cloudflare Worker 프록시(키 Secret·위경도→LCC 격자변환·KV 캐시)가 필요 → "정적·키리스·서버작업 0" 강점(8c) 약화. 단기예보는 +3일뿐이라 7일 주간을 대체 못 함.
- **Open-Meteo `models=kma_*` 옵션은 사용 불가(실측 검증)** — 2026-06-26 기준 `kma_seamless`·`kma_ldps`·`kma_gdps` 모두 current/hourly/daily **전부 null** 반환. 반면 기본 `best_match`는 정상(한국 좌표 자동 최적 모델). 향후 KMA 모델 재시도 전 이 사실 확인할 것.
- **그래서 무비용 개선만 적용** — best_match 유지 + `weather_code` 한글 라벨로 비/소나기/눈 구분 명확화(이모지만으론 헷갈림).

## 미해결 / 다음 시작점
1. (보류 결정됨) 기상청 공식값이 꼭 필요해지면 → Worker 프록시로 15084084 하이브리드 도입.
2. 두물머리 새 폴리곤이 인접 조안면·팔당호 광주 구역과 **중복** — 정리 결정(이전 세션 미결).
3. 다른 금지구역(이포보·여주보·강천보·경안천) 현장도 받으면 정밀화([[wlz-zone-digitizing]]).

## 메모
- KMA 모델 null은 Open-Meteo측 일시적/구조적 문제일 수 있음 — 날짜 지나 재확인 가치 있음.
- 어제(2026-06-25) 분량은 `docs/journal/2026-06-25.md` 참조(하니스 초기화·지형지물·두물머리·위성토글·handoff 자동화).
