# 2026-07-05

## 한 일
- **검색 결과 미리보기 닫힘 보강** — 검색 결과/미리보기 박스(`#srchRes`)가 선택 후 또는 검색창 이탈 후 남는 문제를 수정. 공통 `closeSearchPreview()`를 추가하고 빈 입력, `Esc`, 검색창 blur, 지도 클릭, 결과 없음 타임아웃에서 닫히도록 연결.
- **검색 위치 핀 추가** — 검색 결과 선택 시 지도 이동만 하지 않고 검색 전용 임시 핀(`📍`)을 꽂아 사용자가 선택 위치를 즉시 확인할 수 있게 변경. 일반 주소/장소 검색은 핀 팝업에 외부지도 링크를 유지.

## 검증
- `python3 tools/build_map.py`
- `python3 tools/build_map.py test`
- `PYTHONPYCACHEPREFIX=.pycache_check python3 -m py_compile tools/*.py`
- `node --check /tmp/mycanoe_map_script.js`
