# 2026-09-08

## 한 일

- 기존 마이카누 지도에 `▶ 투어 시작` 진입 버튼을 추가하고, `tour.crowdbase.kr`에서 동작할 투어 전용 산출물 `tour/index.html`을 분리했다.
- 투어 시작 시 등록 코스 드롭다운 대신 지도에서 출발지와 도착지를 순서대로 찍어 예정 코스를 지정하게 바꿨다.
  두 점을 물길로 연결해 거리와 경로를 확인한 후 시작하며, 코스 없이 자유 투어를 시작하는 선택도 유지했다.
- 현재 코스명, 활동 시간, 거리, 평균 속도, 코스 진행률을 모바일 하단 카드에 표시한다. 선택 코스는 파란 점선, GPS 실측은 빨간 실선, 추정 구간은 주황 점선으로 구분했다.
- 수동 `휴식/투어 재개`와 GPS 이동 상태에 따른 자동 휴식/자동 재개를 추가했다. 휴식 중 시간과 좌표 점프는 활동 시간·거리·평균 속도에서 제외된다.
- 화면 꺼짐/앱 전환 후 복귀하면 현위치를 다시 읽는다. GPS 결측이 있으면 선택 코스 또는 출발지↔현위치 물길로 경로·거리를 추정한다.
- 자유 투어는 종료 시 이동 경로를 새 코스로 함께 저장할 수 있게 했다.
- Worker 트립 데이터에 휴식 경계, 원 GPS, 추정 여부, 선택 코스, 진행률을 보존하고 `tour.crowdbase.kr`을 CORS/OAuth 복귀 허용 출처에 추가했다.
- Cloudflare Worker custom domain은 `crowdbase.kr`의 DNS zone이 Cloudflare 관리가 아니라 적용할 수 없었다. 대신 Cloudflare Pages `mycanoe-tour` 프로젝트에 투어 HTML과 필요 GeoJSON 자원을 배포하고, 후이즈에 `tour` CNAME → `mycanoe-tour.pages.dev`를 등록했다.
- 향후 투어 페이지 갱신을 빠뜨리지 않도록 `tools/deploy_tour.sh`에 빌드·자원 선별·Pages 배포 절차를 고정했다.
- 기존 지도와 투어 사이트가 서로 다른 origin이라 로그인 저장소가 분리되던 문제를 수정했다. 기존 지도에서 투어로
  이동할 때 서명된 로그인 정보를 URL fragment로 한 번 넘기고, 투어 페이지가 복원 직후 주소에서 제거한다.
- 하천 검색이 같은 이름의 선분을 결과에서 다시 하나씩 쪼개던 문제를 수정했다. 인접 선분을 수계 단위로 묶어
  `남한강` 5개 선분과 `한강` 3개 선분이 각각 전체 물줄기 한 건으로 선택되게 했다.
- 검색 선택 물줄기에 전용 상위 pane을 적용해 등록 코스와 다른 선 위에서도 강조선이 가려지지 않게 했다.
- 카카오 개발자센터 JavaScript SDK 허용 도메인에 `https://tour.crowdbase.kr`을 추가했다.

## 결정·근거

- 자동 휴식은 GPS 오차를 이동으로 잘못 보지 않도록 정확도에 따라 20~45m의 이동 판정 폭을 쓴다. 약 3분 정지 시 자동 휴식, 다시 이동이 2회 연속 확인되면 자동 재개한다.
- 자동 휴식은 탐지 시점이 아니라 정지가 시작된 시각으로 소급해 활동 시간을 보정한다.
- 수동 휴식에서는 GPS watch를 중지해 배터리를 아끼고, 자동 휴식에서는 재이동을 감지해야 하므로 watch를 유지한다.
- 웹 한계를 숨기지 않고 실측과 추정을 선 색과 `약` 표시로 분리했다. 시간은 사용자 요청대로 시작~현재 경과에서 휴식만 제외한다.
- 직접 지정한 코스는 사용자가 찍은 출발지→도착지 방향을 진행 방향으로 고정한다. 이전 기록을 복원할 때는 저장된
  진행 방향과 진행률을 그대로 이어간다.

## 검증

- `python3 tools/build_map.py`, `python3 tools/build_map.py test`, `node --check` 2종, `git diff --check` 통과.
- Playwright 모바일 390×844에서 기존 지도 진입 버튼, 투어 모드, 출발·도착 순차 지정, 예정 코스 확인, 지정 방향
  진행률, 계산 실패 후 도착지 재선택, 취소, 자유 투어, 수동 휴식 시간 정지, 자동 휴식/자동 재개, 물길 추정 표시,
  자유 투어 코스 저장 옵션, JS 오류 0건을 확인했다.
- 실제 남한강 두 지점을 지정해 물길 9.59km·61개 경로점이 계산되는 것을 확인했다.
- 분리 origin 로그인 전달 테스트에서 투어 링크에 fragment가 생성되고, 투어 페이지가 사용자 정보를 복원한 뒤
  fragment를 제거하며 로그인 관문을 열지 않는 것을 확인했다.
- 하천 연결망 테스트에서 `남한강` 5/5, `한강` 3/3 선분이 한 결과로 묶이고, 강조 pane의 z-index가 일반
  벡터 레이어보다 높은 `480`인지 확인했다.
- 카카오 개발자 설정을 저장 후 다시 열어 투어 도메인이 유지되는 것을 확인했고, 투어 도메인 Referer로 SDK 요청이
  HTTP 200 및 도메인 허용 응답을 반환하는 것도 확인했다.
- 실제 로그인된 Chrome에서 운영 메인 지도의 `▶ 투어 시작`을 눌러 카카오 인증 화면을 거치지 않고 투어 도메인으로
  이동하는 것을 확인했다. 이동 후 로그인 유지, URL fragment 제거, 로그인 관문 숨김 상태까지 재확인했다.

## 미해결·다음 시작점

- 헤드리스 검증은 GPS 시뮬레이션이므로, 첫 실전에서는 스마트폰으로 시작→이동→자동 휴식→재개→종료→저장을 한 번 확인한다.
