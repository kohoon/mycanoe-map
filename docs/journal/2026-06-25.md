# 2026-06-25

## 한 일
- **하니스 초기화** — 프로젝트 `CLAUDE.md`(행동규약: 5단 아님, 빌드→Playwright검증→커밋→자동배포, 보안·데이터갱신) + 메모리 시드 생성 `[a0cee87]`.
- **방치 Cloudflare PR 정리** — #1·#2·#3 모두 close(자동연결 노이즈, 현 `wrangler.toml`과 충돌·중복).
- **공지 답글 삭제(관리자)** — 백엔드 `replyDelete` 액션(ADMIN_KEY 검증, `t`로 답글 매칭, 멱등) + 답글 옆 삭제 링크 `[747a81c]`.
- **지형지물 개편** — "장애물"→"지형지물" 전면 개명 + 🌊여울·⭐유명지 유형 추가(둘 다 이름 지정, 아이콘에 표시) `[493f7b3]`.
- **위성 토글 회귀 수정** — 빈 `VKEY`·`HRFCO_KEY`로 빌드돼 위성토글·수위 누락된 것 복원 `[f7b422a]`.
- **두물머리 금지구역 보정** — 양평 15.4km 띠 → KCG 현장도(020501) 디지타이징 폴리곤(약 5.7×7.5km) `[4dd6c0d]`.
- **위성 토글 디자인 개선** — Esri/VWorld 버튼을 큰 세그먼트 토글로(13px·패딩↑·둥근 컨테이너·활성 진한 파랑) `[1e425b5]`.
- **handoff 자동화** — 전역 `~/.claude/commands/handoff.md`의 커밋·푸시 단계를 "자동 실행(묻지 않음)"으로 수정 + `~/.claude/settings.json`에 `Bash(git push:*)` 허용 규칙 추가(자동모드 푸시 차단 해소).

## 결정 / 근거
- **CLAUDE.md는 5단 구조 미적용 명시** — 이 레포는 의도된 플랫 스크립트(빌드 임베드→CDN 정적 서빙). 전역 규약의 ingest/parse/store/analyze/serve 강제 금지.
- **메모리는 기획문서 복제 안 함** — `기획문서.md`가 이미 살아있는 정본. 메모리는 포인터·세션연속성·미결정만.
- **Cloudflare PR close 사유** — #2는 worker명 변경 시 OAuth 콜백·Secret 바인딩 단절 위험, #1·#3은 GitHub Pages 정적 서빙과 중복.
- **지형지물 "명소"→"유명지" 개명** — 레이어 토글에 이미 즐겨찾기 "명소"(분홍)가 있어 이름 충돌 방지.
- **키 회귀 원인·교훈** — 빌드 시 `vworld_key.txt`/`hrfco_key.txt`(gitignore)가 없으면 키가 빈 값으로 임베드돼 위성·수위 기능 누락. 키값은 도메인잠금이라 git 히스토리에 노출돼 있어 거기서 복원. **앞으로 빌드 전 키 파일 존재 확인 필수.**
- **두물머리 디지타이징** — KCG는 내수면 금지구역을 좌표로 고시하지 않음(현장도 PNG가 최정밀). 빨강 픽셀 추출→정사각 픽셀 지리참조→OSM 오버레이 자가검증(~100-200m 근사). seg_zone 자동 띠가 본류 따라 과대 표시되는 한계를 폴리곤 교체로 해소. 상세 방법은 메모리 [[wlz-zone-digitizing]].

## 미해결 / 다음 시작점
1. 두물머리 새 폴리곤이 인접 **북한강 조안면·팔당호 광주 구역과 중복** — 정리할지 결정(우선).
2. 다른 금지구역(이포보·여주보·강천보·경안천 등) — 현장도 이미지 받으면 같은 방식으로 정밀화.
3. (백로그) 기획문서 9장 — 카누잉 기록 운영 배포, 커스텀 도메인 등.

## 메모
- 이 세션이 하니스 첫 셋업 — 로컬 디렉토리가 비어 있어 원격 클론으로 복구함(원격은 정상).
- main 직접 push는 자동모드가 사용자 명시 승인(AskUserQuestion/"커밋 푸시") 있을 때만 허용.
- 환경에 Playwright 미설치 → 프런트 검증은 인라인 JS `node --check`로 대체(브라우저 런타임 검증 생략).
