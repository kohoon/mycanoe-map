# 2026-09-09

## 한 일

- `▶ 투어 시작` 도입 후 스마트폰에서 메인 지도 하단에 공백이 생기고 화면이 위로 밀리는 회귀를 보정했다.
- 지도를 문서 흐름에서 분리해 실제 보이는 뷰포트 전체에 고정하고, `body` 스크롤을 막았다.
- 모바일 주소창·하단 안전영역 변화를 `visualViewport`로 감지해 화면 높이와 Leaflet 크기를 다시 맞춘다.
- 요구사항을 다시 확인해 `canoe.crowdbase.kr`의 `▶ 투어 시작` 버튼을 완전히 삭제했다.
  메인에서 투어 주소를 만들거나 로그인 정보를 전달하던 코드도 함께 제거했다.
- 실제 투어 시작·휴식·기록 UI는 `tour.crowdbase.kr`에만 남겼다.
- `tour.crowdbase.kr`을 투어 기록 전용 화면으로 간소화했다. 상단에서는 로그인 상태만 남기고 장소 검색·코스 만들기·공지·카페,
  하단에서는 레이어 패널·줌 버튼을 숨겼다. 내 위치와 투어 시작·휴식·진도·기록은 유지했다.
- 일반 장소·코스·장애물·수위 레이어를 투어 지도에서 제거해 GPS 기록선과 예정 코스가 중심이 되게 했다.
  단, `?course=` 공유 코스로 진입한 경우의 참고선은 유지한다.
- 투어 사이트의 로그인 관문을 GPS 경로·거리·속도, 코스 진행률, 휴식 제외 시간·기록 저장만 소개하는 전용 문구로 바꾸었다.
- 투어에서 닉네임을 누르면 일반 마이페이지 대신 투어 기록을 열도록 바꿔 탐색·관리 UI 진입을 줄였다.
- `viewport-fit=cover`로 확장된 화면에서도 검색·레이어·줌 컨트롤과 투어 하단 카드가 노치·홈 인디케이터와 겹치지 않도록 Leaflet 가장자도 안전영역을 반영했다.

## 결정·근거

- `100%` 높이는 실제 모바일 브라우저의 변하는 주소창 높이와 다를 수 있어 `100dvh` 폴백과 `visualViewport.height` 실측값을 같이 사용한다.
- `viewport-fit=cover` 및 `safe-area-inset-bottom`으로 iPhone 홈 인디케이터 영역과 투어 전용 하단 카드가 겹치지 않게 한다.

## 검증

- `python3 tools/build_map.py`, `python3 tools/build_map.py test`, 메인·투어·테스트 HTML의 `node --check`, `git diff --check`를 통과했다.
- Playwright 모바일에서 메인과 투어 페이지를 각각 390×844 → 390×650 → 844×390 → 390×844로 변경했다.
  모든 단계에서 지도·`body`·문서 높이가 `visualViewport.height`와 일치했고, 문서 스크롤과 하단 공백은 0이었다.
- 주소창 크기 변경·화면 회전 후에도 지도 중심이 유지됐고, JavaScript 오류는 0건이었다.
- 생성된 `index.html`·`map.html`에서 `tourLaunch`, `tripbar`, `투어 시작` 문구, `tour.crowdbase.kr` 링크가 모두 0건인지 확인했다.
- Playwright 모바일 검증에서 메인은 투어 컨트롤·링크 0건이었고, 투어 사이트는 `▶ 투어 시작`을
  누르면 출발지·도착지 지정과 자유 투어 선택 모달이 정상적으로 열리는 것을 확인했다.
- 투어 전용 화면에서 장소 검색·코스 측정·공지·카페·레이어 패널·줌·일반 마이페이지 등 8개 일반 지도 UI가
  모두 숨겨졌고, 장소·승하선·장애물·코스·수위·댐·CCTV 등 7개 일반 레이어가 올라오지 않는 것을 확인했다.
- 같은 빌드의 메인 지도에서는 검색·측정·레이어·줌 컨트롤이 그대로 표시돼 도메인별 분리가 유지됐다.
- 투어 전용 로그인 문구, 투어 시작 모달, 닉네임 클릭 시 `내 기록` 열림을 확인했고 JavaScript 오류는 0건이었다.
