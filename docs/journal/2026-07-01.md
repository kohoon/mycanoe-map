# 2026-07-01

## 한 일
- **지형지물 팝업 재바인딩 버그 수정** — 관리자 인증이 지형지물 로드보다 늦게 될 때 팝업(✏️수정/📍이동/🗑삭제)이 안 뜨던 문제. `_refreshObsPopups()`를 `_setAdmin`에서 호출해 관리자 활성 시 재바인딩 `[e9acce2]`.
- **런칭/랜딩 모달 제목 `#id` 제거** — 사용자에게 무의미한 내부 번호 제거, 이름만 표시 `[e9acce2]`.
- **기획 논의(보류)**: ① 투어(카누잉) 기록 기능 — 기획안 작성 ② 신규 기능 브레인스토밍.
- **Codex 인수인계 문서화** — 기존 Claude Code 방식으로 다시 넘겨도 이어받을 수 있게 `기획문서.md` 상단 기준일·데이터 현황·빌드/검증 하네스·에이전트 작업 방식(7b)·핵심 파일 표를 최신화.
- **폴더 구조 정리** — 루트 공개 산출물(`index.html`/`map.html`/fetch GeoJSON)은 유지하고, 스크립트는 `tools/`, 빌드 입력 데이터는 `data/`, Worker는 `workers/`, Google 연동은 `integrations/google/`, 자동화는 `.github/workflows/`로 분리.
- **Windows/맥북 상시실행 자동화 제거 → GitHub Actions 전환** — `.bat` 래퍼와 Windows Task Scheduler 전제를 제거. 맥북도 항상 켜져 있지 않으므로 launchd 대신 GitHub Actions schedule(`daily-collect.yml`, 09:00 KST)을 도입.

## 결정 / 근거
- **투어 기록 = 신규 아님, 보류** — `test.html`에 실시간 GPS 트립이 ~80% 이미 구현(watchPosition·WakeLock·KV저장·공유·랭킹·통계, Worker `/trip*`). **웹 백그라운드 GPS 한계**(화면 꺼짐=중단)로 2026-06-11 롤백된 상태. 카누는 폰을 방수백에 넣어 실시간 웹추적이 반쪽 → 제대로 하려면 하이브리드 앱(8b) 또는 GPX 가져오기/투어 일지(B+C) 방향. 사용자가 **일단 보류** 결정.
- **신규 기능 우선순위 제안(미착수)**: 🥇 **댐 방류 정보/경보**(강 카누 최대 안전변수, 이미 쓰는 HRFCO 출처에 댐 데이터 있을 가능성) > 🥈 일몰시각(좌표계산·무API)+오프라인 지도(PWA) > 코스 메타·장소 로지스틱(주차/화장실/셔틀).
- **에이전트 왕복 원칙** — Codex에서 작업하더라도 새 구조를 만들지 않고 기존 `CLAUDE.md`/`기획문서.md`/`docs/journal` 양식을 유지한다. 큰 변경은 관련 기능 장과 히스토리를 같은 작업에서 갱신한다.
- **자동 수집 운영 전환** — 일일 수집은 로컬 PC 상시 실행에 의존하지 않고 GitHub Actions가 정본. 카카오 로그인 세션은 `KAKAO_AUTH_STATE_B64` Secret으로 복원한다.

## 미해결 / 다음 시작점
1. **신규 기능 방향 결정** — 댐 방류(안전) 우선 추천. 착수 시 먼저 HRFCO에 댐 방류 데이터 실재 확인.
2. (보류) 투어 기록 — B+C(GPX/일지) 웹 우선 vs A(앱) 병행 결정 필요.
3. (백로그) 코스 난이도·거리·소요 메타, 커스텀 도메인 등.
4. 다음 에이전트 시작 시 `CLAUDE.md` → `기획문서.md` 7b → 오늘 저널 순서로 확인.
5. GitHub Actions Secrets 설정 필요: `KAKAO_AUTH_STATE_B64`, `VWORLD_KEY`, `HRFCO_KEY`, `KAKAO_JS_KEY`, `ADMIN_KEY`. 세션 만료 시 로컬에서 `tools/collect_kakao.py`로 재로그인 후 `KAKAO_AUTH_STATE_B64` 갱신.

## 메모
- EV 기능은 전면 제거됨(2026-06-30 저널 참조).
